# Image Annotation with a Transformer Decoder Network

The network takes an image and the (encoded) english word for the image label as input and tries to predict the next character of the label word.

TODO: Add images.